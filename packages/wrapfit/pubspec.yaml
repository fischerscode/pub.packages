name: wrapfit
description: "A new way to fit children of Wrap."
version: 0.1.0+1
repository: https://github.com/fischerscode/pub.packages/tree/main/packages/wrapfit
issue_tracker: https://github.com/fischerscode/pub.packages/issues

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
